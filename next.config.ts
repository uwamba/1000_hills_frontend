import type { NextConfig } from "next";


/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export', // for Next.js 13+ with App Router (experimental, limited)
  trailingSlash: true, // optional: for clean URLs with .html
};

module.exports = nextConfig;


export default nextConfig;



